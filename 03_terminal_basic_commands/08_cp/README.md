- Create a file named `a` and write your favorite singer's name in it.

- Copy the `a` to a file named `b` and check that both files have the same
  contents.

- Copy the directory `source` to `source copy` including all subfolders
