- Check the help info of the `note.rb` script with

        ./note.rb --help

- Add a new note

- Edit the notes source file

- Open the script's sources with `./note.rb --hack`

- Try to understand what is going on. Remember that we can execute bash
  commands from within ruby using backticks.


